# static-slice — problem

README architecture diagrams are either hand-drawn (rot) or generic auto-layout (ugly, unreadable, unanimated).
Developers need: concise text → beautiful deterministic static SVG (light/dark) via committed assets + `<picture>`, with semantic arch model, excellent errors, offline build, and animation as later SMIL backend on the same IR — without infecting core semantics with GitHub hacks.
