# static-slice — critiques (10-gate design + Gate-10 red-team, resolved)

CRITICAL (all fixed): CLI path traversal → cwd containment; DoS caps diagnostic-only → fatal + 256KB/2000-line + ELK timeout; ELK per-block → single instance + injectable; picture/textPath/nested-svg GitHub risk → flattened icons + midpoint halo + xlink; parser scope vars → stack + unclosed diagnostic; error/warn conflation + check-rewrites → severity + dry-run; validatePath dead → allowlist + readme containment; core→language import → core-owned suggest; dual escapers → single escape w/ control-strip; icon dots/centering → fill-only r≥1.2 + translate-center-scale; happy-path tests → 12 incl. red cases; import side-effects + wrong bin path → run() + fixed bin.
Remaining (ticketed): nested groups (v0 error), 21-icon optical + 3 visual candidates blind review, real-GitHub screenshots, SVGO wiring, Playwright regression.
Verdict: PASS_WITH_CHANGES → changes applied, tests 12/12, build+check fresh.
